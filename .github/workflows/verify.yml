name: Verify

on:
  push:

jobs:
  formatting:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v2

      - uses: actions/setup-node@v6
      - run: yarn install

      - run: yarn format --check

  test:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v2

      - uses: actions/setup-node@v6
      - run: yarn install

      - run: yarn test
